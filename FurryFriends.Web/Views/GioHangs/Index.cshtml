@model FurryFriends.API.Models.DTO.GioHangDTO
@{
    Layout = "_Layout";
    var selectedVoucherIdObj = ViewBag.VoucherId;
    var selectedVoucherId = selectedVoucherIdObj is Guid ? (Guid?)selectedVoucherIdObj : null;
    var selectedVoucherIdStr = selectedVoucherIdObj != null ? selectedVoucherIdObj.ToString() : "";
    var tienSauGiam = ViewBag.TienSauGiam as decimal?;
    var giamGia = ViewBag.GiamGia as decimal?;
    var phiVanChuyen = ViewBag.PhiVanChuyen as decimal?;
    var tongDonHang = ViewBag.TongDonHang as decimal?;
    var tenVoucher = ViewBag.TenVoucher as string;
    var maVoucher = ViewBag.MaVoucher as string;
    var danhSachVouchers = ViewBag.Vouchers as IEnumerable<FurryFriends.API.Models.Voucher>;
    var items = Model?.ChiTietGioHang ?? new List<FurryFriends.API.Models.DTO.GioHangChiTietDTO>();
    decimal subTotal = items.Sum(x => x.ThanhTien);

    // Sử dụng thông tin từ ViewBag nếu có, nếu không thì tính mặc định
    decimal shipping = phiVanChuyen ?? (subTotal >= 500000 ? 0 : 30000);
    decimal grandTotal = tienSauGiam.HasValue ? tienSauGiam.Value : (subTotal + shipping);
    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger">
            <i class="fas fa-exclamation-circle"></i> @TempData["ErrorMessage"]
        </div>
    }

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            <i class="fas fa-check-circle"></i> @TempData["SuccessMessage"]
        </div>
    }
    @if (TempData["Loi"] != null)
    {
        <div class="alert alert-danger">
            @TempData["Loi"]
        </div>
    }

}

@section Styles {
    <style>
        :root {
            --primary-color: #6366f1;
            --secondary-color: #8b5cf6;
            --success-color: #10b981;
            --danger-color: #ef4444;
            --warning-color: #f59e0b;
            --info-color: #06b6d4;
            --light-bg: #f8fafc;
            --dark-text: #1e293b;
            --border-color: #e2e8f0;
            --gradient-primary: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --gradient-success: linear-gradient(135deg, #10b981 0%, #059669 100%);
            --gradient-danger: linear-gradient(135deg, #ef4444 0%, #dc2626 100%);
            --gradient-info: linear-gradient(135deg, #06b6d4 0%, #0891b2 100%);
        }

        .cart-container {
            background: rgba(255,255,255,0.95);
            backdrop-filter: blur(20px);
            border-radius: 30px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
            margin: 2rem auto;
            max-width: 1200px;
            overflow: hidden;
            position: relative;
        }

            .cart-container::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 4px;
                background: var(--gradient-primary);
            }

        .cart-header {
            background: var(--gradient-primary);
            color: #fff;
            padding: 3rem 2rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

            .cart-header h1 {
                font-size: 2.4rem;
                font-weight: 800;
                margin-bottom: .5rem;
            }

        .cart-content {
            padding: 2rem;
        }

        .cart-grid {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 2rem;
        }

        @@media (max-width: 992px) {
            .cart-grid {
                grid-template-columns: 1fr;
            }
        }

        .cart-items {
            background: #fff;
            border-radius: 25px;
            overflow: hidden;
            box-shadow: 0 10px 30px rgba(0,0,0,.08);
        }

        .cart-items-header {
            background: var(--gradient-primary);
            color: #fff;
            padding: 1.25rem 1.5rem;
            font-weight: 700;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .cart-item {
            padding: 1.25rem;
            border-bottom: 1px solid var(--border-color);
            display: grid;
            grid-template-columns: 90px 3fr 120px 100px 120px 70px;
            gap: 1rem;
            align-items: start; /* Thay đổi từ center thành start */
            min-height: 120px; /* Tăng chiều cao tối thiểu */
        }

            .cart-item:last-child {
                border-bottom: none;
            }

        @@media (max-width: 768px) {
            .cart-item {
                grid-template-columns: 80px 1fr;
                grid-auto-rows: minmax(40px,auto);
            }
        }

        .product-image {
            width: 90px;
            height: 90px;
            object-fit: cover;
            border-radius: 16px;
            border: 3px solid var(--border-color);
        }

        .product-title {
            font-weight: 700;
            color: var(--dark-text);
            margin-bottom: .35rem;
            /* Tối ưu hiển thị tên sản phẩm dài - Cách 2: Hiển thị nhiều dòng */
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 2; /* Hiển thị tối đa 2 dòng */
            -webkit-box-orient: vertical;
            line-height: 1.2;
            max-height: 2.4em; /* 2 dòng × 1.2 line-height */
            /* Font size cố định nhỏ hơn */
            font-size: 0.85rem;
            /* Thêm transition cho mượt mà */
            transition: all 0.2s ease;
        }

        /* Responsive font size cho tên sản phẩm */
        .product-title.long-text {
            font-size: clamp(0.65rem, 2vw, 0.85rem);
        }

        .product-title.very-long-text {
            font-size: clamp(0.55rem, 1.5vw, 0.75rem);
        }

        /* Tooltip cho tên sản phẩm dài */
        .product-title-container {
            position: relative;
            cursor: help;
        }

        /* Custom tooltip */
        .custom-tooltip {
            position: fixed;
            background: rgba(0, 0, 0, 0.95);
            color: white;
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            font-size: 0.85rem;
            font-weight: 500;
            white-space: normal;
            z-index: 9999;
            max-width: 450px;
            min-width: 250px;
            word-wrap: break-word;
            word-break: break-word;
            line-height: 1.4;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.4);
            border: 1px solid rgba(255, 255, 255, 0.1);
            pointer-events: none;
            opacity: 0;
            transition: opacity 0.2s ease;
        }

        .custom-tooltip.show {
            opacity: 1;
        }

        /* Ẩn một số thông tin khi tên sản phẩm quá dài */
        .cart-item.compact .product-meta {
            display: none;
        }

        .cart-item.compact .price-display {
            font-size: 0.8rem;
        }

        /* Responsive cho mobile */
        @@media (max-width: 768px) {
            .cart-item {
                grid-template-columns: 80px 1fr;
                grid-auto-rows: minmax(40px,auto);
                gap: 0.5rem;
            }
            
            .product-title {
                font-size: 0.75rem;
                -webkit-line-clamp: 3; /* Hiển thị 3 dòng trên mobile */
                max-height: 3.6em;
            }
        }

        .product-meta {
            color: #64748b;
            font-weight: 500;
        }

        .price-display {
            font-weight: 800;
            color: var(--success-color);
            text-align: center;
        }

        .line-through {
            text-decoration: line-through;
            color: #94a3b8;
            font-weight: 600;
        }

        .summary-card {
            position: sticky;
            top: 90px;
            background: #fff;
            border-radius: 24px;
            box-shadow: 0 15px 35px rgba(0,0,0,.08);
            overflow: hidden;
        }

        .summary-header {
            background: var(--gradient-success);
            color: #fff;
            padding: 1rem 1.25rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: .5px;
        }

        .summary-body {
            padding: 1.25rem;
        }

        .summary-row {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: .6rem 0;
            border-bottom: 1px dashed var(--border-color);
        }

            .summary-row:last-child {
                border-bottom: none;
            }

            .summary-row .d-flex {
                width: 100%;
            }

            .summary-row .btn-sm {
                padding: .25rem .5rem;
                font-size: .75rem;
                border-radius: 8px;
                transition: all .2s ease;
            }

                .summary-row .btn-sm:hover {
                    transform: scale(1.05);
                }

        .summary-total {
            font-size: 1.4rem;
            font-weight: 900;
            color: var(--success-color);
        }

        .checkout-btn {
            width: 100%;
            padding: 1rem 1.25rem;
            border: none;
            border-radius: 12px;
            background: var(--gradient-primary);
            color: #fff;
            font-weight: 800;
            letter-spacing: .5px;
            transition: transform .2s ease, box-shadow .2s ease;
        }

            .checkout-btn:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 20px rgba(99,102,241,.35);
            }

        .free-ship {
            background: #ecfdf5;
            color: #065f46;
            padding: .6rem .9rem;
            border-radius: 10px;
            font-weight: 700;
            font-size: .9rem;
            display: inline-flex;
            align-items: center;
            gap: .5rem;
        }

        /* Polished sections in sidebar */
        .section {
            margin-bottom: 1rem;
        }

        .section-title {
            display: flex;
            align-items: center;
            justify-content: space-between;
            font-weight: 800;
            color: var(--dark-text);
            margin-bottom: .5rem;
        }

            .section-title i {
                color: var(--primary-color);
                margin-right: .5rem;
            }

            .section-title .action {
                border: none;
                padding: .4rem .7rem;
                border-radius: 10px;
                background: var(--gradient-info);
                color: #fff;
                font-weight: 700;
            }

        .address-box {
            background: #f8fafc;
            border: 1px solid var(--border-color);
            border-radius: 14px;
            padding: .75rem .9rem;
            color: #334155;
            line-height: 1.35;
        }

        .input-combo {
            display: flex;
            gap: .5rem;
        }

            .input-combo .form-select, .input-combo .form-control {
                border: 2px solid var(--border-color);
                border-radius: 12px;
            }

            .input-combo .btn {
                border-radius: 12px;
                font-weight: 800;
            }

        /* Fallback modal styles (works even if Bootstrap CSS is missing) */
        .modal {
            display: none;
            position: fixed;
            z-index: 9999;
            left: 0;
            top: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,.55);
        }

            .modal.show {
                display: block;
            }

            .modal .modal-dialog {
                margin: 10vh auto;
                max-width: 600px;
                width: calc(100% - 2rem);
            }

            .modal .modal-content {
                background: #fff;
                border-radius: 16px;
                overflow: hidden;
                box-shadow: 0 25px 60px rgba(0,0,0,.25);
            }

            .modal .modal-header {
                padding: 16px 20px;
                border-bottom: 1px solid var(--border-color);
                display: flex;
                align-items: center;
                gap: .6rem;
            }

            .modal .modal-title {
                font-weight: 900;
                color: var(--dark-text);
                letter-spacing: .2px;
            }

            .modal .modal-subtitle {
                font-size: .9rem;
                color: #64748b;
                margin-top: .15rem;
            }

            .modal .title-wrap {
                display: flex;
                flex-direction: column;
            }

            .modal .title-icon {
                width: 36px;
                height: 36px;
                display: inline-flex;
                align-items: center;
                justify-content: center;
                border-radius: 12px;
                background: var(--gradient-info);
                color: #fff;
            }

            .modal .modal-body {
                padding: 16px 18px;
                max-height: 55vh;
                overflow: auto;
            }

            .modal .modal-footer {
                padding: 12px 18px;
                border-top: 1px solid var(--border-color);
                display: flex;
                justify-content: flex-end;
                gap: .5rem;
            }

        .btn-close {
            background: none;
            border: none;
            width: 24px;
            height: 24px;
            opacity: .65;
            cursor: pointer;
        }

            .btn-close::before {
                content: '\00D7';
                display: block;
                font-size: 24px;
                line-height: 24px;
                color: #64748b;
            }

        .btn {
            padding: .5rem .9rem;
            border-radius: 10px;
            font-weight: 800;
            cursor: pointer;
        }

        .btn-primary {
            background: var(--gradient-primary);
            color: #fff;
            border: none;
        }

            .btn-primary:hover {
                filter: brightness(1.05);
            }

        .btn-outline-secondary {
            background: #fff;
            border: 1.5px solid var(--border-color);
            color: #334155;
        }

            .btn-outline-secondary:hover {
                background: #f8fafc;
            }

        .btn-outline-danger {
            border: 1.5px solid var(--danger-color);
            color: var(--danger-color);
            background: #fff;
        }

            .btn-outline-danger:hover {
                background: var(--danger-color);
                color: #fff;
            }

        .header-actions {
            margin-left: auto;
            display: flex;
            gap: .5rem;
            align-items: center;
        }

        .address-list {
            display: flex;
            flex-direction: column;
            gap: .75rem;
        }

        /* Validation Summary Styles */
        .alert-danger {
            background: linear-gradient(135deg, #fef2f2 0%, #fee2e2 100%);
            border: 2px solid #fecaca;
            border-left: 4px solid #ef4444;
            color: #991b1b;
            border-radius: 12px;
            padding: 1rem 1.25rem;
            margin-bottom: 1rem;
            animation: slideInDown 0.3s ease-out;
        }

            .alert-danger i {
                color: #ef4444;
            }

            .alert-danger strong {
                color: #7f1d1d;
            }

        @@keyframes slideInDown {
            from {
                opacity: 0;
                transform: translateY(-10px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .address-item {
            display: flex;
            gap: .85rem;
            align-items: flex-start;
            background: #fff;
            border-radius: 14px;
            transition: all .2s ease;
        }

            .address-item:hover {
                transform: translateY(-2px);
            }

        .address-radio {
            appearance: none;
            width: 20px;
            height: 20px;
            min-width: 20px;
            border: 2px solid #94a3b8;
            border-radius: 50%;
            margin-top: .55rem;
            cursor: pointer;
            position: relative;
        }

            .address-radio:checked {
                border-color: #6366f1;
                background: radial-gradient(#6366f1 55%, transparent 56%);
            }

        .address-content {
            flex: 1;
            border: 1.5px solid var(--border-color);
            border-radius: 14px;
            padding: .85rem 1rem;
            box-shadow: 0 6px 14px rgba(0,0,0,.06);
        }

        .address-radio:checked + .address-content {
            border-color: #6366f1;
            background: linear-gradient(180deg,#ffffff, #f8fafc);
            box-shadow: 0 10px 24px rgba(99,102,241,.18);
        }

        .address-name {
            font-weight: 900;
            color: #0f172a;
            display: flex;
            align-items: center;
            gap: .5rem;
        }

        .address-default {
            padding: .2rem .5rem;
            background: #eef2ff;
            color: #4f46e5;
            border-radius: 9999px;
            font-size: .75rem;
            font-weight: 800;
        }

        .address-line {
            color: #475569;
            font-size: .95rem;
            margin-top: .15rem;
        }

        /* ✅ Validation message styling */
        .validation-message {
            margin-top: 0.5rem;
            margin-bottom: 0.5rem;
        }

            .validation-message .text-danger {
                color: var(--danger-color) !important;
                font-size: 0.875rem;
                font-weight: 600;
                display: flex;
                align-items: center;
                gap: 0.5rem;
            }

        /* Beautiful Notification Styles */
        .beautiful-notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background-color: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            padding: 15px 20px;
            display: none;
            z-index: 10000;
            max-width: 350px;
            opacity: 0;
            transform: translateY(-20px);
            transition: opacity 0.3s ease-in-out, transform 0.3s ease-in-out;
            border-left: 4px solid #007bff; /* Default color */
        }

            .beautiful-notification.show {
                display: block;
                opacity: 1;
                transform: translateY(0);
            }

            .beautiful-notification.beautiful-notification-success {
                border-left-color: #28a745;
            }

            .beautiful-notification.beautiful-notification-error {
                border-left-color: #dc3545;
            }

            .beautiful-notification.beautiful-notification-info {
                border-left-color: #17a2b8;
            }

            .beautiful-notification.beautiful-notification-warning {
                border-left-color: #ffc107;
            }

        .notification-header {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 8px;
        }

        .notification-icon {
            font-size: 1.2rem;
            margin-right: 10px;
        }

        .notification-title {
            font-weight: 700;
            font-size: 1rem;
        }

        .notification-close {
            background: none;
            border: none;
            font-size: 1.2rem;
            cursor: pointer;
            color: #6c757d;
            padding: 0 5px;
        }

        .notification-body {
            font-size: 0.9rem;
            color: #495057;
        }
    </style>
}

<div class="cart-container">
    <div class="cart-header">
        <h1><i class="fas fa-shopping-cart"></i> Giỏ hàng của bạn</h1>
        <p class="opacity-90">Kiểm tra sản phẩm và tiến hành thanh toán</p>
    </div>

    <div class="cart-content">
        @if (Model == null || Model.ChiTietGioHang == null || !Model.ChiTietGioHang.Any())
        {
            <div class="empty-cart text-center p-10 bg-[var(--light-bg)] rounded-2xl">
                <i class="fas fa-shopping-cart text-5xl text-[var(--primary-color)] mb-4"></i>
                <h3 class="text-2xl font-bold text-[var(--dark-text)] mb-2">Giỏ hàng trống</h3>
                <p class="text-gray-500 mb-6">Hãy khám phá thêm sản phẩm thú vị dành cho thú cưng của bạn.</p>
                <a href="@Url.Action("Index", "SanPhamKhachHang")" class="px-6 py-3 rounded-xl text-white font-semibold" style="background:var(--gradient-primary)">Tiếp tục mua sắm</a>
            </div>
        }
        else
        {
            <div class="cart-grid" id="cartGrid">
                <div>
                    <div class="cart-items">
                        <div class="cart-items-header">
                            <span><i class="fas fa-list-ul"></i> Sản phẩm (@Model.ChiTietGioHang.Count() mục)</span>
                            <a href="@Url.Action("Index", "SanPhamKhachHang")" class="text-white/90 hover:text-white"><i class="fas fa-plus-circle"></i> Thêm sản phẩm</a>
                        </div>
                        @foreach (var item in Model.ChiTietGioHang)
                        {
                            <div class="cart-item">
                                <img src="@($"https://localhost:7289/images/{System.IO.Path.GetFileName(item.AnhSanPham ?? "")}")" class="product-image" alt="@item.TenSanPham" />
                                <div>
                                    <div class="product-title-container" data-full-title="@item.TenSanPham">
                                        <div class="product-title">@item.TenSanPham</div>
                                    </div>
                                    <div class="product-meta">Màu: @item.MauSac | Size: @item.KichCo</div>
                                </div>
                                <div class="text-center">
                                    @if (item.GiaGoc > 0 && item.GiaGoc != item.DonGia)
                                    {
                                        <div class="line-through">@item.GiaGoc.ToString("N0") VNĐ</div>
                                    }
                                    <div class="price-display">@item.DonGia.ToString("N0") VNĐ</div>
                                </div>
                                <div class="text-center">
                                    <input type="number" value="@item.SoLuong" min="1" class="quantity-input" data-id="@item.GioHangChiTietId" onchange="updateQuantity('@item.GioHangChiTietId', this.value)" style="width:90px; text-align:center; border:2px solid var(--border-color); border-radius:12px; padding:.5rem; font-weight:700;" />
                                </div>
                                <div class="text-center">
                                    <div class="price-display item-total" data-id="@item.GioHangChiTietId">@item.ThanhTien.ToString("N0") VNĐ</div>
                                </div>
                                <div class="text-center flex justify-start items-center">
                                    <form asp-action="Remove" method="post" class="inline-block">
                                        <input type="hidden" name="chiTietId" value="@item.GioHangChiTietId" />
                                        <input type="hidden" name="voucherId" value="@selectedVoucherId" />
                                        <button type="submit" class="px-4 py-3 rounded-xl text-white" style="background:var(--gradient-danger); display:inline-flex; align-items:center; justify-content:center;" title="Xóa sản phẩm" onclick="return confirm('Xóa sản phẩm này?')">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </form>
                                </div>
                            </div>
                        }
                    </div>
                </div>
                <aside>
                    <div class="summary-card">
                        <div class="summary-header"><i class="fas fa-receipt"></i> Thông tin đơn hàng</div>
                        <div class="summary-body">
                            <div class="section">
                                <div class="section-title">
                                    <div><i class="fas fa-location-dot"></i> Địa chỉ giao hàng</div>
                                    <button id="btnChooseAddress" type="button" class="action" onclick="(function(){var m=document.getElementById('addressModal'); if(!m) return; m.classList.add('show'); m.style.display='block'; m.removeAttribute('aria-hidden');})(); return false;"><i class="fas fa-location-dot"></i> Thay đổi</button>
                                </div>
                                <div id="shippingAddressText" class="address-box">
                                    @{
                                        var dcs = (IEnumerable<FurryFriends.API.Models.DiaChiKhachHang>)(ViewBag.DiaChis ?? new List<FurryFriends.API.Models.DiaChiKhachHang>());
                                        var macDinh = dcs.FirstOrDefault(x => x.TrangThai == 1);
                                        if (macDinh != null)
                                        {
                                            @:@macDinh.TenDiaChi - @macDinh.MoTa, @macDinh.PhuongXa, @macDinh.ThanhPho
                                        }
                                        else
                                        {
                                            @:Chưa chọn địa chỉ
                                        }
                                    }
                                </div>
                                <!-- ✅ Validation message cho địa chỉ giao hàng -->
                                <div class="validation-message">
                                    @Html.ValidationMessage("DiaChiGiaoHangId", "", new { @class = "text-danger" })
                                </div>
                                <input type="hidden" name="DiaChiGiaoHangId" id="diaChiIdHidden" />
                                <!-- Modal chọn địa chỉ -->
                                <div class="modal fade" id="addressModal" tabindex="-1" aria-hidden="true">
                                    <div class="modal-dialog modal-dialog-centered">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <div class="title-icon"><i class="fas fa-location-dot"></i></div>
                                                <div class="title-wrap">
                                                    <h5 class="modal-title">Chọn địa chỉ giao hàng</h5>
                                                    <div class="modal-subtitle">Hãy chọn địa chỉ bạn muốn nhận hàng</div>
                                                </div>
                                                <div class="header-actions">
                                                    <button type="button" class="btn-close" onclick="(function(){var m=document.getElementById('addressModal'); if(!m) return; m.classList.remove('show'); m.style.display='none'; m.setAttribute('aria-hidden','true');})(); return false;"></button>
                                                </div>
                                            </div>
                                            <div class="modal-body">
                                                <div class="address-list">
                                                    @foreach (var dc in (IEnumerable<FurryFriends.API.Models.DiaChiKhachHang>)(ViewBag.DiaChis ?? new List<FurryFriends.API.Models.DiaChiKhachHang>()))
                                                    {
                                                        <label class="address-item">
                                                            <input class="address-radio" type="radio" name="diaChiRadio" value="@dc.DiaChiId" @(dc.TrangThai == 1 ? "checked" : "") data-default="@(dc.TrangThai == 1)" />
                                                            <div class="address-content">
                                                                <div class="address-name">@dc.TenDiaChi @if(dc.TrangThai == 1){
                                                                <span class="address-default">Mặc định</span>
                                                            }
    </div>
                                                            <div class="address-line">@dc.MoTa, @dc.PhuongXa, @dc.ThanhPho</div>
                                                        </div>
                                                    </label>
                                                }
                                                @if ((ViewBag.DiaChis == null) || ((IEnumerable<FurryFriends.API.Models.DiaChiKhachHang>)ViewBag.DiaChis).Count() == 0)
                                                {
                                                    <div class="text-muted">Bạn chưa có địa chỉ. Vui lòng thêm trong hồ sơ.</div>
                                                }
                                            </div>
                                        </div>
                                        <div class="modal-footer">
                                            <button type="button" class="btn btn-outline-secondary" onclick="(function(){var m=document.getElementById('addressModal'); if(!m) return; m.classList.remove('show'); m.style.display='none'; m.setAttribute('aria-hidden','true');})(); return false;">Đóng</button>
                                            <button type="button" class="btn btn-primary" id="btnSelectAddressConfirm" onclick="(function(){var c=document.querySelector('input[name=diaChiRadio]:checked'); if(!c) return; var it=c.closest('.address-item'); var t=(it&&it.querySelector('.address-line')?it.querySelector('.address-line').textContent:''); var n=(it&&it.querySelector('.address-name')?it.querySelector('.address-name').textContent:''); var box=document.getElementById('shippingAddressText'); if(box) box.textContent=n+' - '+t; var hid=document.getElementById('diaChiIdHidden'); if(hid) hid.value=c.value; var m=document.getElementById('addressModal'); if(m){ m.classList.remove('show'); m.style.display='none'; m.setAttribute('aria-hidden','true'); }})(); return false;">Chọn</button>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <div class="section">
                            <div class="section-title">
                                <div><i class="fas fa-ticket"></i> Voucher</div>
                                <button type="button" class="btn btn-link text-sm p-0" id="btnShowAvailableVouchers">
                                    <i class="fas fa-list"></i> Xem voucher khả dụng
                                </button>
                            </div>
                            <div class="input-combo">
                                <input type="text" id="voucherInput" class="form-control flex-1" placeholder="Nhập mã voucher (ví dụ: WELCOME20)" />
                                <button id="btnApplyVoucher" class="btn btn-primary px-4">
                                    <i class="fas fa-check"></i> Áp dụng
                                </button>
                            </div>
                            <div id="voucherResult" class="mt-2" style="display: none;">
                                <div id="voucherSuccess" class="alert alert-success p-2 mb-0" style="display: none;">
                                    <i class="fas fa-check-circle"></i> <span id="voucherSuccessText"></span>
                                </div>
                                <div id="voucherError" class="alert alert-danger p-2 mb-0" style="display: none;">
                                    <i class="fas fa-exclamation-circle"></i> <span id="voucherErrorText"></span>
                                </div>
                            </div>
                            <!-- Không đặt name=VoucherId ở đây để tránh trùng với input trong form submit -->
                            <input type="hidden" id="selectedVoucherId" />
                        </div>

                        <div class="section">
                            <div class="section-title"><div><i class="fas fa-wallet"></i> Hình thức thanh toán</div></div>
                            <select id="paymentSelect" class="form-select">
                                @foreach (var hinhThuc in (IEnumerable<FurryFriends.API.Models.HinhThucThanhToan>)ViewBag.HinhThucThanhToanList)
                                {
                                    <option value="@hinhThuc.HinhThucThanhToanId">@hinhThuc.TenHinhThuc</option>
                                }
                            </select>
                        </div>
                        <div class="summary-row">
                            <span>Tạm tính</span>
                            <strong>@subTotal.ToString("N0") VNĐ</strong>
                        </div>
                        @if (ViewBag.GiamGia != null && ViewBag.GiamGia > 0)
                        {
                            <div class="summary-row">
                                <div class="d-flex justify-content-between align-items-center w-100">
                                    <span>Giảm giá (voucher @maVoucher)</span>
                                    <div class="d-flex align-items-center gap-2">
                                        <strong class="text-[var(--danger-color)]">-@ViewBag.GiamGia.ToString("N0") VNĐ</strong>
                                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="removeVoucher()" title="Hủy áp dụng voucher">
                                            <i class="fas fa-times"></i>
                                        </button>
                                    </div>
                                </div>
                            </div>
                        }
                        <div class="summary-row">
                            <span>Phí vận chuyển</span>
                            <strong>@(shipping == 0 ? "Miễn phí" : shipping.ToString("N0") + " VNĐ")</strong>
                        </div>
                        <div class="summary-row">
                            <span class="free-ship"><i class="fas fa-truck"></i> Miễn phí đơn từ 500.000 VNĐ</span>
                        </div>
                        <div class="summary-row">
                            <span class="summary-total">Tổng thanh toán</span>
                            <span class="summary-total">@grandTotal.ToString("N0") VNĐ</span>
                        </div>
                        <div class="mt-4">
                            <!-- Validation Summary -->
                            <div asp-validation-summary="All" class="alert alert-danger mb-3" role="alert" style="display: @(ViewData.ModelState.IsValid ? "none" : "block")">
                                <i class="fas fa-exclamation-triangle me-2"></i>
                                <strong>Vui lòng kiểm tra các lỗi sau:</strong>
                            </div>

                            <form asp-action="ThanhToan" method="post" id="checkoutForm">
                                <input type="hidden" name="KhachHangId" value="@ViewBag.KhachHangId" />
                                <input type="hidden" name="VoucherId" id="voucherIdHidden" value="@selectedVoucherIdStr" />
                                <input type="hidden" name="TaiKhoanId" value="@Guid.Empty" />
                                <input type="hidden" name="HinhThucThanhToanId" id="paymentIdHidden" />
                                <input type="hidden" name="DiaChiGiaoHangId" id="diaChiIdHidden_2" />
                                <button type="submit" class="checkout-btn"><i class="fas fa-credit-card"></i> Tiến hành thanh toán</button>
                            </form>
                        </div>
                    </div>
                </div>
            </aside>
        </div>
                }
    </div>
</div>

<!-- Voucher Modal -->
<div class="modal fade" id="voucherModal" tabindex="-1" aria-labelledby="voucherModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="voucherModalLabel">
                    <i class="fas fa-tickets-alt"></i> Voucher khả dụng
                </h5>
                <button type="button" class="btn-close ms-auto" data-bs-dismiss="modal" aria-label="Close" onclick="closeVoucherModal()"></button>
            </div>
            <div class="modal-body">
                <div id="voucherLoadingState" class="text-center py-4">
                    <i class="fas fa-spinner fa-spin"></i> Đang tải danh sách voucher...
                </div>
                <div id="voucherList" style="display: none;"></div>
                <div id="voucherEmptyState" style="display: none;" class="text-center py-4 text-muted">
                    <i class="fas fa-ticket-alt fa-2x mb-3"></i>
                    <p>Không có voucher khả dụng cho đơn hàng này</p>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        // Expose to global for inline onchange calls
        window.updateQuantity = function(id, qty){
            const form = document.createElement('form');
            form.method = 'POST';
            form.action = '/GioHangs/UpdateQuantity';
            const inputId = document.createElement('input'); inputId.type='hidden'; inputId.name='chiTietId'; inputId.value=id; form.appendChild(inputId);
            const inputQty = document.createElement('input'); inputQty.type='hidden'; inputQty.name='soLuong'; inputQty.value=parseInt(qty||1); form.appendChild(inputQty);
            const inputVoucher = document.createElement('input'); inputVoucher.type='hidden'; inputVoucher.name='voucherId'; inputVoucher.value='@selectedVoucherId'; form.appendChild(inputVoucher);
            document.body.appendChild(form);
            form.submit();
        };

        function openAddressModal(){
            const addressModalEl = document.getElementById('addressModal');
            if (!addressModalEl) return;
            if (window.bootstrap && bootstrap.Modal) {
                const instance = bootstrap.Modal.getOrCreateInstance(addressModalEl, { backdrop: 'static' });
                instance.show();
            } else {
                addressModalEl.classList.add('show');
                addressModalEl.style.display = 'block';
                addressModalEl.removeAttribute('aria-hidden');
            }
        }

        document.addEventListener('DOMContentLoaded', function(){
            // ✅ Tự động điều chỉnh font size cho tên sản phẩm khi trang load
            adjustProductTitleFontSize();
            
            // ✅ Khởi tạo tooltip tùy chỉnh
            initCustomTooltips();
            
            // ✅ Điều chỉnh lại khi resize window
            window.addEventListener('resize', function() {
                adjustProductTitleFontSize();
            });
            
            // Đồng bộ badge giỏ hàng thực tế mỗi khi vào trang này
            if (window.refreshCartCount) window.refreshCartCount();

            // Tự động cập nhật số lượng sau 400ms khi người dùng dừng gõ
            const debounce = (fn, delay = 400) => { let t; return (...args) => { clearTimeout(t); t = setTimeout(() => fn(...args), delay); }; };

            function updateQuantity(id, qty){
                // Gọi qua MVC controller để luôn đúng base URL, tránh CORS/API port
                const form = document.createElement('form');
                form.method = 'POST';
                form.action = '/GioHangs/UpdateQuantity';
                const inputId = document.createElement('input'); inputId.type='hidden'; inputId.name='chiTietId'; inputId.value=id; form.appendChild(inputId);
                const inputQty = document.createElement('input'); inputQty.type='hidden'; inputQty.name='soLuong'; inputQty.value=parseInt(qty||1); form.appendChild(inputQty);
                const inputVoucher = document.createElement('input'); inputVoucher.type='hidden'; inputVoucher.name='voucherId'; inputVoucher.value='@selectedVoucherId'; form.appendChild(inputVoucher);
                document.body.appendChild(form);
                form.submit();
            }

            document.querySelectorAll('.quantity-input').forEach(inp=>{
                const id = inp.dataset.id;
                const handler = debounce(()=> updateQuantity(id, inp.value));
                inp.addEventListener('input', handler);
                inp.addEventListener('change', ()=> updateQuantity(id, inp.value));
            });

            // đã bỏ nút sync; tự động cập nhật khi thay đổi số lượng

            // Modal địa chỉ
            const addressModalEl = document.getElementById('addressModal');
            let addressModalInstance = null;
            const hideModal = ()=>{
                if (!addressModalEl) return;
                if (window.bootstrap && bootstrap.Modal) {
                    addressModalInstance?.hide();
                } else {
                    addressModalEl.classList.remove('show');
                    addressModalEl.style.display = 'none';
                    addressModalEl.setAttribute('aria-hidden','true');
                }
            };
            document.getElementById('btnSelectAddressConfirm')?.addEventListener('click', ()=>{
                const checked = document.querySelector("input[name='diaChiRadio']:checked");
                if(!checked){ return; }
                const item = checked.closest('.address-item');
                const text = item?.querySelector('.address-line')?.textContent || '';
                const name = item?.querySelector('.address-name')?.textContent || '';
                document.getElementById('shippingAddressText').textContent = `${name} - ${text}`;
                // set hidden
                const hid = document.getElementById('diaChiIdHidden'); if (hid) hid.value = checked.value;
                const hid2 = document.getElementById('diaChiIdHidden_2'); if (hid2) hid2.value = checked.value;
                hideModal();
            });

            // Set mặc định từ radio đang checked (địa chỉ mặc định) ngay khi load
            const checkedRadio = document.querySelector("input[name='diaChiRadio']:checked");
            if (checkedRadio) {
                const hid = document.getElementById('diaChiIdHidden'); if (hid) hid.value = checkedRadio.value;
                const hid2 = document.getElementById('diaChiIdHidden_2'); if (hid2) hid2.value = checkedRadio.value;
            }

            // Cập nhật hidden ngay khi người dùng thay đổi lựa chọn trong modal
            document.querySelectorAll("input[name='diaChiRadio']").forEach(r => {
                r.addEventListener('change', () => {
                    const hid = document.getElementById('diaChiIdHidden'); if (hid) hid.value = r.value;
                    const hid2 = document.getElementById('diaChiIdHidden_2'); if (hid2) hid2.value = r.value;
                });
            });

            // Voucher functionality
            let currentCartTotal = @(subTotal.ToString("F0", System.Globalization.CultureInfo.InvariantCulture));
            let selectedVoucherId = null;

            // Apply voucher
            document.getElementById('btnApplyVoucher')?.addEventListener('click', async function() {
                const code = (document.getElementById('voucherInput')?.value || '').trim();
                const btn = this;
                const originalText = btn.innerHTML;

                if (!code) {
                    showVoucherResult(false, 'Vui lòng nhập mã voucher');
                    return;
                }

                btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Kiểm tra...';
                btn.disabled = true;

                try {
                    const response = await fetch('https://localhost:7289/api/VoucherValidation/validate', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json', 'X-Requested-With': 'XMLHttpRequest' },
                        body: JSON.stringify({
                            voucherCode: code,
                            tongTienHang: currentCartTotal
                        })
                    });

                    const result = await response.json();

                    if (result.success) {
                        selectedVoucherId = result.voucherId;
                        const sel = document.getElementById('selectedVoucherId');
                        if (sel) sel.value = result.voucherId;
                        const hidden = document.getElementById('voucherIdHidden');
                        if (hidden) hidden.value = result.voucherId;
                        showVoucherResult(true, result.message);
                        // Reload trang với voucherId để server tính lại tổng
                        const url = new URL(window.location.href);
                        url.searchParams.set('voucherId', result.voucherId);
                        window.location.href = url.toString();
                    } else {
                        selectedVoucherId = null;
                        const sel = document.getElementById('selectedVoucherId');
                        if (sel) sel.value = '';
                        showVoucherResult(false, result.message);
                        // Loại bỏ voucherId khỏi URL nếu đang có để không áp dụng sai
                        const url = new URL(window.location.href);
                        if (url.searchParams.has('voucherId')) {
                            url.searchParams.delete('voucherId');
                            window.location.href = url.toString();
                        }
                    }
                } catch (error) {
                    showVoucherResult(false, 'Có lỗi xảy ra khi kiểm tra voucher');
                } finally {
                    btn.innerHTML = originalText;
                    btn.disabled = false;
                }
            });

            // Show available vouchers
            document.getElementById('btnShowAvailableVouchers')?.addEventListener('click', function() {
                showAvailableVouchers();
            });

            function showVoucherResult(success, message) {
                const resultDiv = document.getElementById('voucherResult');
                const successDiv = document.getElementById('voucherSuccess');
                const errorDiv = document.getElementById('voucherError');
                const successText = document.getElementById('voucherSuccessText');
                const errorText = document.getElementById('voucherErrorText');

                resultDiv.style.display = 'block';

                if (success) {
                    successDiv.style.display = 'block';
                    errorDiv.style.display = 'none';
                    successText.textContent = message;
                } else {
                    successDiv.style.display = 'none';
                    errorDiv.style.display = 'block';
                    errorText.textContent = message;
                }
            }

            async function showAvailableVouchers() {
                const modalEl = document.getElementById('voucherModal');
                if (window.bootstrap && bootstrap.Modal) {
                    const modal = new bootstrap.Modal(modalEl);
                    modal.show();
                } else {
                    // Fallback nếu thiếu Bootstrap JS
                    modalEl.style.display = 'block';
                    modalEl.classList.add('show');
                    modalEl.removeAttribute('aria-hidden');
                }

                document.getElementById('voucherLoadingState').style.display = 'block';
                document.getElementById('voucherList').style.display = 'none';
                document.getElementById('voucherEmptyState').style.display = 'none';

                try {
                    const khachHangId = '@(ViewBag.KhachHangId ?? Guid.Empty)';
                    const response = await fetch(`https://localhost:7289/api/VoucherValidation/available/${khachHangId}?tongTienHang=${currentCartTotal}`, { headers: { 'X-Requested-With': 'XMLHttpRequest' } });
                    const result = await response.json();

                    document.getElementById('voucherLoadingState').style.display = 'none';

                    if (result.success && result.vouchers.length > 0) {
                        displayVoucherList(result.vouchers);
                    } else {
                        document.getElementById('voucherEmptyState').style.display = 'block';
                    }
                } catch (error) {
                    document.getElementById('voucherLoadingState').style.display = 'none';
                    document.getElementById('voucherEmptyState').style.display = 'block';
                }
            }

            function displayVoucherList(vouchers) {
                const container = document.getElementById('voucherList');
                container.innerHTML = '';

                vouchers.forEach(voucher => {
                    const voucherCard = document.createElement('div');
                    voucherCard.className = 'voucher-card mb-3 p-3 border rounded';
                    voucherCard.innerHTML = `
                        <div class="d-flex justify-content-between align-items-start">
                            <div class="flex-grow-1">
                                <h6 class="mb-1 fw-bold text-primary">${voucher.tenVoucher} <small class="text-muted">(${voucher.maVoucher ?? ''})</small></h6>
                                <p class="mb-2 text-muted">Giảm ${voucher.phanTramGiam}%${voucher.giaTriGiamToiDa ? ` (tối đa ${voucher.giaTriGiamToiDa.toLocaleString()} VNĐ)` : ''}</p>
                                ${voucher.soTienApDungToiThieu ? `<small class="text-info">Áp dụng cho đơn từ ${voucher.soTienApDungToiThieu.toLocaleString()} VNĐ</small><br>` : ''}
                                <small class="text-success">Tiết kiệm: ${voucher.soTienGiam.toLocaleString()} VNĐ</small>
                            </div>
                            <button class="btn btn-primary btn-sm ms-3" onclick="selectVoucher('${voucher.voucherId}', '${voucher.tenVoucher}', '${voucher.maVoucher ?? ''}')">
                                Chọn
                            </button>
                        </div>
                    `;
                    container.appendChild(voucherCard);
                });

                container.style.display = 'block';
            }

            window.selectVoucher = function(voucherId, voucherName, voucherCode) {
                document.getElementById('voucherInput').value = voucherName;
                const sel = document.getElementById('selectedVoucherId');
                if (sel) sel.value = voucherId;
                const hidden = document.getElementById('voucherIdHidden');
                if (hidden) hidden.value = voucherId;
                selectedVoucherId = voucherId;
                if (voucherCode) {
                    document.getElementById('voucherInput').value = voucherCode;
                }

                closeVoucherModal();

                // Chuyển trang với voucherId để áp dụng ngay
                const url = new URL(window.location.href);
                url.searchParams.set('voucherId', voucherId);
                window.location.href = url.toString();
            };

            window.closeVoucherModal = function() {
                const modalEl = document.getElementById('voucherModal');
                const instance = (window.bootstrap && bootstrap.Modal) ? bootstrap.Modal.getInstance(modalEl) : null;
                if (instance) instance.hide();
                // Fallback
                modalEl.style.display='none';
                modalEl.classList.remove('show');
                modalEl.removeAttribute('aria-hidden');
            }

            // Hủy áp dụng voucher
            window.removeVoucher = function() {
                if (confirm('Bạn có chắc chắn muốn hủy áp dụng voucher này?')) {
                    // Xóa voucher khỏi các input hidden
                    const sel = document.getElementById('selectedVoucherId');
                    if (sel) sel.value = '';
                    const hidden = document.getElementById('voucherIdHidden');
                    if (hidden) hidden.value = '';
                    selectedVoucherId = null;

                    // Xóa voucherId khỏi URL và reload trang
                    const url = new URL(window.location.href);
                    if (url.searchParams.has('voucherId')) {
                        url.searchParams.delete('voucherId');
                        window.location.href = url.toString();
                    } else {
                        // Nếu không có voucherId trong URL, reload trang để reset
                        window.location.reload();
                    }
                }
            }

            // Bind hình thức thanh toán vào hidden
            const select = document.getElementById('paymentSelect');
            const hiddenPayment = document.getElementById('paymentIdHidden');
            if (select && hiddenPayment) {
                const sync = ()=> hiddenPayment.value = select.value;
                select.addEventListener('change', sync); sync();
            }

            // ✅ Validation form thanh toán
            const checkoutForm = document.getElementById('checkoutForm');
            if (checkoutForm) {
                checkoutForm.addEventListener('submit', function(e) {
                    const diaChiId = document.getElementById('diaChiIdHidden').value;
                    const diaChiId2 = document.getElementById('diaChiIdHidden_2').value;

                    // Kiểm tra xem có địa chỉ nào được chọn không
                    if (!diaChiId && !diaChiId2) {
                        e.preventDefault();
                        showBeautifulNotification('Vui lòng chọn địa chỉ giao hàng trước khi thanh toán!', 'error');
                        return false;
                    }

                    // Kiểm tra hình thức thanh toán
                    const hinhThucThanhToan = document.getElementById('paymentIdHidden').value;
                    if (!hinhThucThanhToan) {
                        e.preventDefault();
                        showBeautifulNotification('Vui lòng chọn hình thức thanh toán!', 'error');
                        return false;
                    }
                    
                    // ✅ Kiểm tra hình thức thanh toán có hợp lệ không
                    const allowedPaymentMethods = [
                        'aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa', // Thanh toán khi nhận hàng
                        'bbbbbbbb-bbbb-bbbb-bbbb-bbbbbbbbbbbb'  // Thanh toán VNPay
                    ];
                    
                    if (!allowedPaymentMethods.includes(hinhThucThanhToan)) {
                        e.preventDefault();
                        showBeautifulNotification('Chỉ hỗ trợ thanh toán khi nhận hàng hoặc thanh toán VNPay!', 'error');
                        return false;
                    }
                });
            }

            // ✅ Hàm tạo và quản lý tooltip tùy chỉnh
            function initCustomTooltips() {
                let tooltip = null;
                let timeout = null;

                // Tạo tooltip element
                function createTooltip() {
                    if (tooltip) return tooltip;
                    tooltip = document.createElement('div');
                    tooltip.className = 'custom-tooltip';
                    document.body.appendChild(tooltip);
                    return tooltip;
                }

                // Hiển thị tooltip
                function showTooltip(element, text) {
                    console.log('🔍 Showing tooltip for:', text); // Debug log
                    const tooltipEl = createTooltip();
                    tooltipEl.textContent = text;
                    tooltipEl.classList.add('show');

                    const rect = element.getBoundingClientRect();
                    const tooltipRect = tooltipEl.getBoundingClientRect();
                    
                    // Tính toán vị trí
                    let left = rect.left;
                    let top = rect.top - tooltipRect.height - 10;

                    // Kiểm tra nếu tooltip bị tràn ra ngoài màn hình
                    if (left + tooltipRect.width > window.innerWidth) {
                        left = window.innerWidth - tooltipRect.width - 10;
                    }
                    if (left < 10) left = 10;
                    if (top < 10) {
                        top = rect.bottom + 10;
                    }

                    tooltipEl.style.left = left + 'px';
                    tooltipEl.style.top = top + 'px';
                    
                    console.log('🔍 Tooltip positioned at:', left, top); // Debug log
                }

                // Ẩn tooltip
                function hideTooltip() {
                    if (tooltip) {
                        tooltip.classList.remove('show');
                    }
                }

                // Xử lý sự kiện cho các product title
                document.querySelectorAll('.product-title-container').forEach(container => {
                    const title = container.querySelector('.product-title');
                    if (!title) return;

                    const fullText = container.getAttribute('data-full-title');
                    
                    // Luôn hiển thị tooltip khi có fullText
                    if (fullText) {
                        container.addEventListener('mouseenter', () => {
                            clearTimeout(timeout);
                            timeout = setTimeout(() => {
                                showTooltip(container, fullText);
                            }, 300); // Delay 300ms
                        });

                        container.addEventListener('mouseleave', () => {
                            clearTimeout(timeout);
                            hideTooltip();
                        });
                    }
                });
            }

            // ✅ Hàm tự động điều chỉnh font size cho tên sản phẩm
            function adjustProductTitleFontSize() {
                const cartItems = document.querySelectorAll('.cart-item');
                
                cartItems.forEach(item => {
                    const title = item.querySelector('.product-title');
                    if (!title) return;
                    
                    const text = title.textContent || title.innerText;
                    const textLength = text.length;
                    
                    // Xóa class cũ
                    title.classList.remove('long-text', 'very-long-text');
                    item.classList.remove('compact');
                    
                    // Thêm class dựa trên độ dài text
                    if (textLength > 80) {
                        title.classList.add('very-long-text');
                        item.classList.add('compact'); // Ẩn thông tin phụ khi tên quá dài
                    } else if (textLength > 60) {
                        title.classList.add('very-long-text');
                    } else if (textLength > 40) {
                        title.classList.add('long-text');
                    }
                    
                    // Điều chỉnh font size động nếu cần
                    if (textLength > 100) {
                        title.style.fontSize = '0.45rem';
                    } else if (textLength > 80) {
                        title.style.fontSize = '0.5rem';
                    } else if (textLength > 60) {
                        title.style.fontSize = '0.6rem';
                    } else if (textLength > 40) {
                        title.style.fontSize = '0.7rem';
                    } else {
                        title.style.fontSize = '';
                    }
                });
            }

            // ✅ Hàm hiển thị thông báo đẹp
            function showBeautifulNotification(message, type = 'info') {
                // Xóa thông báo cũ nếu có
                const existingNotification = document.querySelector('.beautiful-notification');
                if (existingNotification) {
                    existingNotification.remove();
                }

                // Tạo thông báo mới
                const notification = document.createElement('div');
                notification.className = `beautiful-notification beautiful-notification-${type}`;

                const icon = type === 'error' ? '' : type === 'success' ? '✅' : 'ℹ️';
                const title = type === 'error' ? 'Lỗi' : type === 'success' ? 'Thành công' : 'Thông báo';

                notification.innerHTML = `
                    <div class="notification-header">
                        <span class="notification-icon">${icon}</span>
                        <span class="notification-title">${title}</span>
                        <button class="notification-close" onclick="this.parentElement.parentElement.remove()">×</button>
                    </div>
                    <div class="notification-body">
                        ${message}
                    </div>
                `;

                // Thêm vào body
                document.body.appendChild(notification);

                // Hiển thị với animation
                setTimeout(() => {
                    notification.classList.add('show');
                }, 10);

                // Tự động ẩn sau 5 giây (trừ khi là lỗi)
                if (type !== 'error') {
                    setTimeout(() => {
                        notification.classList.remove('show');
                        setTimeout(() => notification.remove(), 300);
                    }, 5000);
                }
            }
        });
    </script>
}
