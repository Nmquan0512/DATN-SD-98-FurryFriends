@model FurryFriends.API.Models.LoginRequest
@{
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
    ViewData["Title"] = "Đăng nhập quản trị";
}
<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FurryFriends - Đăng nhập quản trị</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            overflow-x: hidden;
        }

        /* Animated background elements */
        .bg-shapes {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .shape {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            animation: float 6s ease-in-out infinite;
        }

        .shape:nth-child(1) {
            width: 80px;
            height: 80px;
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }

        .shape:nth-child(2) {
            width: 60px;
            height: 60px;
            top: 60%;
            right: 10%;
            animation-delay: 2s;
        }

        .shape:nth-child(3) {
            width: 40px;
            height: 40px;
            bottom: 20%;
            left: 20%;
            animation-delay: 4s;
        }

        @@keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-20px) rotate(180deg); }
        }

        .login-container {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            border-radius: 24px;
            padding: 40px;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 420px;
            position: relative;
            z-index: 2;
            animation: slideUp 0.8s ease-out;
        }

        @@keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .logo-section {
            text-align: center;
            margin-bottom: 30px;
        }

        .logo {
            font-size: 36px;
            font-weight: bold;
            background: linear-gradient(135deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 8px;
            position: relative;
        }

        .logo::after {
            content: '🐾';
            position: absolute;
            right: -30px;
            top: 0;
            animation: bounce 2s infinite;
        }

        @@keyframes bounce {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-10px); }
            60% { transform: translateY(-5px); }
        }

        .subtitle {
            color: #666;
            font-size: 14px;
            margin-bottom: 20px;
        }

        .welcome-text {
            font-size: 24px;
            font-weight: 600;
            color: #333;
            margin-bottom: 8px;
        }

        .form-group {
            margin-bottom: 20px;
            position: relative;
        }

        .form-group label {
            display: block;
            margin-bottom: 8px;
            color: #555;
            font-weight: 500;
            font-size: 14px;
        }

        .form-group input {
            width: 100%;
            padding: 16px 20px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            font-size: 16px;
            transition: all 0.3s ease;
            background: #fafbfc;
        }

        .form-group input:focus {
            outline: none;
            border-color: #667eea;
            background: white;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
            transform: translateY(-2px);
        }

        .form-group input:hover {
            border-color: #667eea;
        }

        .password-toggle {
            position: absolute;
            right: 16px;
            top: 38px;
            cursor: pointer;
            color: #888;
            font-size: 16px;
            transition: all 0.3s ease;
            padding: 2px;
        }

        .password-toggle:hover {
            color: #667eea;
            transform: scale(1.1);
        }

        .forgot-password {
            text-align: right;
            margin-bottom: 24px;
        }

        .forgot-password a {
            color: #667eea;
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s ease;
        }

        .forgot-password a:hover {
            color: #764ba2;
            text-decoration: underline;
        }

        .login-btn {
            width: 100%;
            padding: 16px;
            background: linear-gradient(135deg, #667eea, #764ba2);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .login-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.5s;
        }

        .login-btn:hover::before {
            left: 100%;
        }

        .login-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
        }

        .login-btn:active {
            transform: translateY(0);
        }

        .divider {
            display: flex;
            align-items: center;
            margin: 24px 0;
            color: #888;
            font-size: 14px;
        }

        .divider::before,
        .divider::after {
            content: '';
            flex: 1;
            height: 1px;
            background: #e1e5e9;
        }

        .divider span {
            padding: 0 16px;
        }

        .social-login {
            display: flex;
            gap: 12px;
            margin-bottom: 24px;
        }

        .social-btn {
            flex: 1;
            padding: 12px;
            border: 2px solid #e1e5e9;
            border-radius: 12px;
            background: white;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 20px;
            text-align: center;
        }

        .social-btn:hover {
            border-color: #667eea;
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .signup-link {
            text-align: center;
            color: #666;
            font-size: 14px;
        }

        .signup-link a {
            color: #667eea;
            text-decoration: none;
            font-weight: 600;
            transition: color 0.3s ease;
        }

        .signup-link a:hover {
            color: #764ba2;
        }

        .walking-cat {
            position: absolute;
            top: -45px;
            width: 80px;
            height: 55px;
            animation: catWalk 12s ease-in-out infinite;
            z-index: 10;
        }

        .walking-cat img {
            width: 100%;
            height: 100%;
            object-fit: contain;
        }

        @@keyframes catWalk {
            0% {
                left: 20px;
                transform: scaleX(1);
            }
            45% {
                left: calc(100% - 100px);
                transform: scaleX(1);
            }
            50% {
                left: calc(100% - 100px);
                transform: scaleX(-1);
            }
            95% {
                left: 20px;
                transform: scaleX(-1);
            }
            100% {
                left: 20px;
                transform: scaleX(1);
            }
        }

        /* Responsive design */
        @@media (max-width: 480px) {
            .login-container {
                margin: 20px;
                padding: 30px 20px;
            }
            
            .logo {
                font-size: 28px;
            }
            
            .welcome-text {
                font-size: 20px;
            }
        }

        /* Loading animation */
        .loading {
            display: none;
            width: 20px;
            height: 20px;
            border: 2px solid #ffffff;
            border-top: 2px solid transparent;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-right: 10px;
        }

        @@keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* ✅ Hệ thống thông báo đẹp */
        .notification-container {
            position: fixed;
            top: 20px;
            right: 20px;
            z-index: 1000;
            max-width: 400px;
        }

        /* ✅ Cải thiện thông báo popup */
        .notification {
            position: fixed;
            top: 20px;
            right: 20px;
            background: white;
            border-radius: 12px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.12);
            padding: 0;
            margin: 10px 0;
            z-index: 1000;
            max-width: 400px;
            transform: translateX(100%);
            animation: slideInRight 0.5s ease forwards;
            border-left: 4px solid;
        }

        .notification.success {
            border-left-color: #10b981;
        }

        .notification.error {
            border-left-color: #ef4444;
        }

        .notification.warning {
            border-left-color: #f59e0b;
        }

        .notification.info {
            border-left-color: #3b82f6;
        }

        .notification-content {
            display: flex;
            align-items: center;
            padding: 16px 20px;
            gap: 12px;
        }

        .notification-icon {
            font-size: 20px;
            flex-shrink: 0;
        }

        .notification.success .notification-icon {
            color: #10b981;
        }

        .notification.error .notification-icon {
            color: #ef4444;
        }

        .notification.warning .notification-icon {
            color: #f59e0b;
        }

        .notification.info .notification-icon {
            color: #3b82f6;
        }

        .notification-message {
            flex: 1;
            color: #374151;
            font-size: 14px;
            line-height: 1.4;
            font-weight: 500;
        }

        .notification-close {
            background: none;
            border: none;
            color: #9ca3af;
            cursor: pointer;
            padding: 4px;
            border-radius: 6px;
            transition: all 0.2s ease;
            flex-shrink: 0;
        }

        .notification-close:hover {
            background: #f3f4f6;
            color: #6b7280;
        }

        .notification.hide {
            animation: slideOutRight 0.5s ease forwards;
        }

        @@keyframes slideInRight {
            from {
                transform: translateX(100%);
                opacity: 0;
            }
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        @@keyframes slideOutRight {
            from {
                transform: translateX(0);
                opacity: 1;
            }
            to {
                transform: translateX(100%);
                opacity: 0;
            }
        }

        /* ✅ Thông báo inline trong form */
        .form-notification {
            margin-bottom: 20px;
            padding: 16px;
            border-radius: 12px;
            border: none;
            font-weight: 500;
            text-align: center;
            animation: fadeInUp 0.5s ease-out;
        }

        .form-notification.success {
            background: linear-gradient(135deg, #d1fae5, #a7f3d0);
            color: #065f46;
            border-left: 4px solid #10b981;
        }

        .form-notification.error {
            background: linear-gradient(135deg, #fee2e2, #fecaca);
            color: #991b1b;
            border-left: 4px solid #ef4444;
        }

        .form-notification.warning {
            background: linear-gradient(135deg, #fef3c7, #fde68a);
            color: #92400e;
            border-left: 4px solid #f59e0b;
        }

        @@keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        /* ✅ Cải thiện validation messages */
        .field-validation-error {
            color: #ef4444;
            font-size: 12px;
            margin-top: 4px;
            display: block;
            animation: shake 0.5s ease-in-out;
        }

        .input-validation-error {
            border-color: #ef4444 !important;
            box-shadow: 0 0 0 3px rgba(239, 68, 68, 0.1) !important;
        }

        .input-validation-success {
            border-color: #10b981 !important;
            box-shadow: 0 0 0 3px rgba(16, 185, 129, 0.1) !important;
        }

        @@keyframes shake {
            0%, 100% { transform: translateX(0); }
            25% { transform: translateX(-5px); }
            75% { transform: translateX(5px); }
        }

        /* ✅ Loading state cho button */
        .login-btn.loading {
            pointer-events: none;
            opacity: 0.7;
        }

        .login-btn.loading .loading {
            display: inline-block;
        }

        .login-btn.loading span:not(.loading) {
            display: none;
        }
    </style>
</head>
<body>
    <div class="bg-shapes">
        <div class="shape"></div>
        <div class="shape"></div>
        <div class="shape"></div>
    </div>

    <!-- ✅ Container thông báo popup -->
    <div class="notification-container" id="notificationContainer"></div>

    <div class="login-container">
        <div class="walking-cat">
            <img src="https://media4.giphy.com/media/v1.Y2lkPTc5MGI3NjExdHBtcjdycHN5YjNrZmpqcHM3Zm1wN3hsbm84aTU3d3M4cTJsMmRxdyZlcD12MV9pbnRlcm5hbF9naWZfYnlfaWQmY3Q9cw/jlw3OMYrH6mb4loQJI/giphy.gif" alt="Walking Cat">
        </div>
        <div class="logo-section">
            <div class="logo">Đăng nhập vào Quản Lý - FurryFriends</div>
            <div class="subtitle">Cửa hàng thú cưng yêu thương</div>
        </div>
        <p style="color: #666; margin-bottom: 24px; font-size: 14px;">Đăng nhập để truy cập hệ thống quản lý</p>
        
        <!-- ✅ Thông báo inline trong form -->
        @if (TempData["Success"] != null)
        {
            <div class="form-notification success" id="successMessage">
                <i class="bi bi-check-circle-fill"></i>
                @Html.Raw(TempData["Success"])
            </div>
        }
        @if (TempData["Warning"] != null)
        {
            <div class="form-notification warning" id="warningMessage">
                <i class="bi bi-exclamation-circle-fill"></i>
                @Html.Raw(TempData["Warning"])
            </div>
        }

        <form asp-action="DangNhap" method="post" id="loginForm">
            <div class="form-group">
                <label for="UserName">Tài Khoản</label>
                <input asp-for="UserName" class="form-control" placeholder="Nhập tài khoản" required />
                <span asp-validation-for="UserName" class="field-validation-error"></span>
            </div>
            <div class="form-group" style="position: relative;">
                <label for="Password">Mật khẩu</label>
                <input asp-for="Password" type="password" class="form-control" placeholder="Nhập mật khẩu" required />
                <i class="bi bi-eye password-toggle" onclick="togglePassword()" id="toggleIcon"></i>
                <span asp-validation-for="Password" class="field-validation-error"></span>
            </div>
            <div class="forgot-password">
                <a href="#" onclick="showForgotPassword()">Quên mật khẩu?</a>
            </div>
            <button type="submit" class="login-btn" id="loginBtn">
                <span class="loading"></span>
                Đăng nhập
            </button>
        </form>

        <!-- ✅ Thông báo popup đẹp -->
        @if (TempData["Success"] != null)
        {
            <div class="notification success" id="popupSuccess">
                <div class="notification-content">
                    <i class="bi bi-check-circle-fill notification-icon"></i>
                    <div class="notification-message">@Html.Raw(TempData["Success"])</div>
                    <button class="notification-close" onclick="closeNotification('popupSuccess')">
                        <i class="bi bi-x"></i>
                    </button>
                </div>
            </div>
        }
        @if (TempData["Error"] != null)
        {
            <div class="notification error" id="popupError">
                <div class="notification-content">
                    <i class="bi bi-exclamation-triangle-fill notification-icon"></i>
                    <div class="notification-message">@Html.Raw(TempData["Error"])</div>
                    <button class="notification-close" onclick="closeNotification('popupError')">
                        <i class="bi bi-x"></i>
                    </button>
                </div>
            </div>
        }
        @if (TempData["Warning"] != null)
        {
            <div class="notification warning" id="popupWarning">
                <div class="notification-content">
                    <i class="bi bi-exclamation-circle-fill notification-icon"></i>
                    <div class="notification-message">@Html.Raw(TempData["Warning"])</div>
                    <button class="notification-close" onclick="closeNotification('popupWarning')">
                        <i class="bi bi-x"></i>
                    </button>
                </div>
            </div>
        }
    </div>

    <script src="~/lib/jquery/jquery.min.js"></script>
    <script src="~/lib/jquery-validation/jquery.validate.min.js"></script>
    <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>

    <script>
        // ✅ Hàm hiển thị thông báo popup
        function showNotification(message, type = 'info') {
            const container = document.getElementById('notificationContainer');
            const notification = document.createElement('div');
            notification.className = `notification ${type}`;
            
            const icons = {
                success: 'bi-check-circle-fill',
                error: 'bi-exclamation-triangle-fill',
                warning: 'bi-exclamation-circle-fill',
                info: 'bi-info-circle-fill'
            };
            
            notification.innerHTML = `
                <i class="bi ${icons[type]} notification-icon"></i>
                <div class="notification-message">${message}</div>
                <button class="notification-close" onclick="this.parentElement.remove()">
                    <i class="bi bi-x"></i>
                </button>
            `;
            
            container.appendChild(notification);
            
            // Tự động ẩn sau 5 giây
            setTimeout(() => {
                notification.classList.add('hide');
                setTimeout(() => notification.remove(), 500);
            }, 5000);
        }

        function togglePassword() {
            var passwordInput = document.querySelector('input[name="Password"]');
            var icon = document.getElementById('toggleIcon');
            if (passwordInput.type === "password") {
                passwordInput.type = "text";
                icon.classList.remove('bi-eye');
                icon.classList.add('bi-eye-slash');
            } else {
                passwordInput.type = "password";
                icon.classList.remove('bi-eye-slash');
                icon.classList.add('bi-eye');
            }
        }

        // ✅ Cải thiện form validation
        $(document).ready(function() {
            // Real-time validation feedback
            $('input').on('blur', function() {
                if (this.checkValidity()) {
                    $(this).removeClass('input-validation-error').addClass('input-validation-success');
                } else {
                    $(this).removeClass('input-validation-success').addClass('input-validation-error');
                }
            });

            // Form submission
            $('#loginForm').on('submit', function(e) {
                const btn = $('#loginBtn');
                btn.addClass('loading');
                
                // Kiểm tra validation
                if (!this.checkValidity()) {
                    e.preventDefault();
                    btn.removeClass('loading');
                    showNotification('Vui lòng kiểm tra lại thông tin!', 'warning');
                    return false;
                }
                
                // Nếu có thông báo lỗi từ server, hiển thị nó
                const errorMessage = $('.form-notification.error').text();
                if (errorMessage && errorMessage.trim()) {
                    showNotification(errorMessage, 'error');
                }
            });
        });

        // ✅ Tự động ẩn thông báo inline (chỉ success và warning, không ẩn error)
        setTimeout(function() {
            const successNotifications = document.querySelectorAll('.form-notification.success, .form-notification.warning');
            successNotifications.forEach(notification => {
                notification.style.opacity = '0';
                notification.style.transform = 'translateY(-20px)';
                setTimeout(() => notification.remove(), 300);
            });
        }, 5000);

        // ✅ Đóng thông báo popup
        function closeNotification(id) {
            const notification = document.getElementById(id);
            if (notification) {
                notification.classList.add('hide');
                setTimeout(() => notification.remove(), 500);
            }
        }

        // ✅ Tự động ẩn popup (chỉ success và warning, không ẩn error)
        setTimeout(function() {
            const successPopups = document.querySelectorAll('.notification.success, .notification.warning, .notification.info');
            successPopups.forEach(popup => {
                popup.classList.add('hide');
                setTimeout(() => popup.remove(), 500);
            });
        }, 8000);

        // ✅ Các hàm tiện ích
        function showForgotPassword() {
            showNotification('Chức năng quên mật khẩu sẽ được triển khai sau! 🐾', 'info');
        }
    </script>

    @section Scripts { }
</body>
</html> 